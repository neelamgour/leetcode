class Solution {
public:
    int titleToNumber(string s) {
       long int num=0;
       int i;
       len=s.length();
        
        for(i=0;i<len;++i)
            num = num*26 + s[i]-'A'+1;
     return num;
    }
};
